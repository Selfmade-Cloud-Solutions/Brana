import 'package:flutter/material.dart';

// import '../../../../constants.dart';

class Walk3DarkCardContent extends StatelessWidget {
  const Walk3DarkCardContent({super.key});

  @override
  Widget build(BuildContext context) {
    return  Image.asset(
    "assets/images/walk3.png",
    height: 500,
    width: 500,
    );
  }
}
